# Port for the server to run on
PORT=5000

# MongoDB connection string
# For local MongoDB: mongodb://localhost:27017/personal-finance-tracker
# For MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/personal-finance-tracker
MONGODB_URI=mongodb://localhost:27017/personal-finance-tracker

# Environment
NODE_ENV=development

# JWT Configuration
# Generate a strong secret key for JWT tokens (use a random string generator)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRE=7d

# CORS Configuration
# Add allowed origins separated by commas for cross-origin requests
# For development: http://localhost:3000
# For production: https://your-frontend-domain.com,https://another-domain.com
ALLOWED_ORIGINS=http://localhost:3000